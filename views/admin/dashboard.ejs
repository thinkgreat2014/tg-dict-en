<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - InnovateTech Solutions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .admin-nav {
            background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
        }
        .section-card {
            transition: transform 0.2s;
        }
        .section-card:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body class="bg-light">
    <!-- Admin Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark admin-nav">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/admin">
                <i class="fas fa-cog me-2"></i>Admin Dashboard
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/" target="_blank">
                    <i class="fas fa-external-link-alt me-1"></i>View Site
                </a>
                <a class="nav-link" href="/admin/logout">
                    <i class="fas fa-sign-out-alt me-1"></i>Logout
                </a>
            </div>
        </div>
    </nav>

    <div class="container-fluid py-4">
        <div class="row mb-4">
            <div class="col">
                <h2 class="fw-bold">Content Management</h2>
                <p class="text-muted">Update your website content below</p>
            </div>
        </div>

        <!-- Hero Section -->
        <div class="row mb-4">
            <div class="col">
                <div class="card section-card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0"><i class="fas fa-home me-2"></i>Hero Section</h5>
                    </div>
                    <div class="card-body">
                        <form action="/admin/hero" method="POST">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="hero-title" class="form-label">Title</label>
                                    <input type="text" class="form-control" id="hero-title" name="title" value="<%= data.hero.title %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="hero-button" class="form-label">Button Text</label>
                                    <input type="text" class="form-control" id="hero-button" name="buttonText" value="<%= data.hero.buttonText %>" required>
                                </div>
                            </div>
                            <div class="mb-3">
                                <label for="hero-subtitle" class="form-label">Subtitle</label>
                                <textarea class="form-control" id="hero-subtitle" name="subtitle" rows="2" required><%= data.hero.subtitle %></textarea>
                            </div>
                            <div class="mb-3">
                                <label for="hero-image" class="form-label">Background Image URL</label>
                                <input type="url" class="form-control" id="hero-image" name="backgroundImage" value="<%= data.hero.backgroundImage %>" required>
                            </div>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save me-2"></i>Update Hero Section
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!-- About Section -->
        <div class="row mb-4">
            <div class="col">
                <div class="card section-card">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0"><i class="fas fa-info-circle me-2"></i>About Section</h5>
                    </div>
                    <div class="card-body">
                        <form action="/admin/about" method="POST">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="about-title" class="form-label">Title</label>
                                    <input type="text" class="form-control" id="about-title" name="title" value="<%= data.about.title %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="about-image" class="form-label">Image URL</label>
                                    <input type="url" class="form-control" id="about-image" name="image" value="<%= data.about.image %>" required>
                                </div>
                            </div>
                            <div class="mb-3">
                                <label for="about-description" class="form-label">Description</label>
                                <textarea class="form-control" id="about-description" name="description" rows="3" required><%= data.about.description %></textarea>
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="about-mission" class="form-label">Mission</label>
                                    <textarea class="form-control" id="about-mission" name="mission" rows="3" required><%= data.about.mission %></textarea>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="about-vision" class="form-label">Vision</label>
                                    <textarea class="form-control" id="about-vision" name="vision" rows="3" required><%= data.about.vision %></textarea>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-save me-2"></i>Update About Section
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!-- Services Section -->
        <div class="row mb-4">
            <div class="col">
                <div class="card section-card">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="mb-0"><i class="fas fa-cogs me-2"></i>Services</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <% data.services.forEach((service, index) => { %>
                            <div class="col-md-4 mb-4">
                                <div class="border rounded p-3">
                                    <h6 class="fw-bold text-warning">Service <%= index + 1 %></h6>
                                    <form action="/admin/services" method="POST">
                                        <input type="hidden" name="serviceIndex" value="<%= index %>">
                                        <div class="mb-2">
                                            <label class="form-label">Title</label>
                                            <input type="text" class="form-control form-control-sm" name="title" value="<%= service.title %>" required>
                                        </div>
                                        <div class="mb-2">
                                            <label class="form-label">Description</label>
                                            <textarea class="form-control form-control-sm" name="description" rows="2" required><%= service.description %></textarea>
                                        </div>
                                        <div class="mb-2">
                                            <label class="form-label">Icon (Emoji)</label>
                                            <input type="text" class="form-control form-control-sm" name="icon" value="<%= service.icon %>" required>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Image URL</label>
                                            <input type="url" class="form-control form-control-sm" name="image" value="<%= service.image %>" required>
                                        </div>
                                        <button type="submit" class="btn btn-warning btn-sm">
                                            <i class="fas fa-save me-1"></i>Update
                                        </button>
                                    </form>
                                </div>
                            </div>
                            <% }) %>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Contact Section -->
        <div class="row mb-4">
            <div class="col">
                <div class="card section-card">
                    <div class="card-header bg-info text-white">
                        <h5 class="mb-0"><i class="fas fa-envelope me-2"></i>Contact Information</h5>
                    </div>
                    <div class="card-body">
                        <form action="/admin/contact" method="POST">
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <label for="contact-address" class="form-label">Address</label>
                                    <textarea class="form-control" id="contact-address" name="address" rows="2" required><%= data.contact.address %></textarea>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label for="contact-phone" class="form-label">Phone</label>
                                    <input type="text" class="form-control" id="contact-phone" name="phone" value="<%= data.contact.phone %>" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <label for="contact-email" class="form-label">Email</label>
                                    <input type="email" class="form-control" id="contact-email" name="email" value="<%= data.contact.email %>" required>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-info">
                                <i class="fas fa-save me-2"></i>Update Contact Info
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>